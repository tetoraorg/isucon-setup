#!/bin/bash

set -e

echo "
Check list
- pprofを切る
- goのログを切る
- goのDB待ち
Will
- slow-query-logを切る
- fluent-bitを切る
- ソースコードのビルド
"

slow-off

sudo systemctl stop fluent-bit

pwd=$PWD
cd $PROJECT_ROOT/go && go build -o $APP_NAME ./...
cd $pwd
