#!/bin/bash

set -e

if [ -s $PROJECT_ROOT/etc/isuumo.go.service ]; then
  echo "moving service file ..."
  sudo cp $PROJECT_ROOT/etc/isuumo.go.service /etc/systemd/system
  sudo systemctl daemon-reload
fi