#!/bin/bash

set -e

if [ -s $PROJECT_ROOT/etc/$SERVICE_NAME.service ]; then
  echo "moving service file ..."
  sudo cp $PROJECT_ROOT/etc/$SERVICE_NAME.service /etc/systemd/system
  sudo systemctl daemon-reload
fi